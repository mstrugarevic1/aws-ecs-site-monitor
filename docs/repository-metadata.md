# Repository Metadata

Suggested GitHub description:

```text
HTTP monitoring service for AWS ECS/Fargate with FastAPI, Scheduler, Worker, SQS, DynamoDB, SNS, Terraform and CI validation.
```

Suggested topics:

```text
aws, ecs, fargate, fastapi, terraform, dynamodb, sqs, sns, cloudwatch, github-actions, monitoring, python, docker
```
