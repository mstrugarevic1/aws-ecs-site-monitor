# Repository Metadata

Suggested GitHub description:

```text
Site Monitor: lightweight HTTP endpoint monitoring on AWS ECS/Fargate with FastAPI, Scheduler, Worker, SQS, DynamoDB, SNS, Terraform and CI validation.
```

Suggested topics:

```text
aws, ecs, fargate, fastapi, terraform, dynamodb, sqs, sns, cloudwatch, github-actions, monitoring, site-monitor, python, docker
```
